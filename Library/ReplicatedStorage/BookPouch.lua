local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Class = require(ReplicatedStorage.Utilities.Class)

local BookPouch = Class.new("BookPouch")

function BookPouch.new(books)
end

return BookPouch